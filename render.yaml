services:
  - type: web
    name: agri-assistant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DASHSCOPE_API_KEY
        sync: false
      - key: PORT
        value: 8000
